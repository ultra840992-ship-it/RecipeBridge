- **DB 연동 확인**: 매칭 상태가 DB에 정확히 저장/로드되는지 확인.
- **테스트 방법**: `curl` 또는 Postman으로 `/api/match_status` 엔드포인트에 `GET`/`POST` 요청을 직접 보내 상태 변경 및 조회가 정상 작동하는지 검증한다.

### 3.2 프론트엔드 (`index.js`)
- **실시간 폴링 로직 확인**: `index.js`에서 주기적으로 백엔드의 `/api/match_status`를 호출하여 상태 변화를 감지하는 로직이 있는지 확인.
  - **확인 스니펫**:
    